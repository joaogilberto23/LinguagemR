# Script teste em R


install.packages("caret")
# Forma de instalar pacotes no ambiente de trabalho.

library(caret)
# Sempre que for utilizar um pacote específico, é necessário chamá-lo antes.

library(ggplot2)

help(ggplot2) | ?getwd
# Duas formas de consultar a documentação do R sobre determinada função.